name: Awareness Email Campaign Gate

on:
  push:
    branches:
      - awareness-email-campaigns
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: npm
      - name: Install server dependencies
        run: npm install --prefix server
      - name: Install client dependencies
        run: npm install --prefix client
      - name: Run awareness campaign tests
        run: |
          cd server
          npx mocha tests/AwarenessEmailCampaignService.test.js tests/AwarenessTemplateGalleryService.test.js tests/AwarenessMailDeliveryService.test.js --exit
      - name: Build client
        run: npm run build --prefix client
      - name: Syntax checks
        run: |
          node --check server/services/awareness/AwarenessEmailCampaignService.js
          node --check server/routes/scorm/awarenessGallery.js
          node --check server/models/scorm/ScormAwarenessEmailCampaign.js
          node --check server/models/scorm/ScormAwarenessEmailCampaignRecipient.js
