{
  "dependencies": {
    "redis": "^5.11.0",
    "redis-adapter": "^0.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  },
  "scripts": {
    "test:e2e": "playwright test",
    "test:critical": "cd server && npm run test && cd ../client && npm run build && cd .. && npm run test:e2e"
  }
}
